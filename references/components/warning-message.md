Warning message (slug warning-message, family inline-message, sibling info-message, status beta, last updated 2026-09-08) is persistent context about consequences or risks before someone continues. It is a source-based recreation of the page-authored warning banner in the Designer app's content-import confirmation modal (modules/projects/components/runtime-content/import/kb-import-confirmation-modal.component.html:14), reviewed against Designer checkout 753c84252b on 2026-09-08, not a Figma extraction, and it has no Figma link. Its props are Warning message (page markup: a fixed banner in Review Content Import shown when breakingCount is nonzero, with no component props, size enum, title slot, or dismissal input), content (projected content in the site's rendered recreation; the reference banner has fixed text), and width ("fill" or "fit", default "fill", exposed by the shared rendered component as width 100% or fit-content with max-width 100% and min-width 0, though the Warning message page only shows fill and width follows the caller's panel). The documentation preview offers only preview width (Wide, Narrow; default Wide, where Narrow caps only the documentation canvas at 320px). Content variations are Plain (reference copy "These keys are referenced by build query criteria and will be removed. Affected builds will fail until resolved."), Emphasis (the same body with "fail" in bold, a documentation recommendation rather than part of the source template), and Wrapping (the reference body in a 260px-wide column, wrapping to several lines with the icon at the top). Width variations are Full panel and Half-width column, both using the copy "Settings are not configured, some functionality may be missing." There is no title slot (the Breaking Changes heading belongs to the surrounding section), no action inside the message (Import Anyway belongs to the surrounding modal), and no dismiss control or timer. Seven observed app variations exist in Designer (the breaking-changes reference itself; settings and operation warnings with 8px padding, centered alignment and Neutral 500 text; email settings with 12px corners and half-width placement; dataset and capture warnings with 8px or 4px padding and centered 12px / 16px Neutral 500 text; an API-key reset warning with 12px padding, 14px / 20px Neutral 700 text, a 2px icon offset and no visible border width; a migration warning with a stronger Orange 500 border; and a React provider warning with a 0.5px Orange 100 border, 14px horizontal and 8px vertical padding, a 10px gap and 12px Neutral 900 text); these are caller differences, not a supported variant API. The message has no interactive states and no size axis.

Its anatomy is a Container (pale orange surface, 12px padding on every side, 8px corner radius, 1px border, horizontal flex row aligned to the top with an 8px gap), an IconSlot (fixed 16x16, flex-centered, non-shrinking), an Icon (Phosphor Warning at regular weight, 16x16, aligned to the start of the content, aria-hidden), and a Body (12px / 16px text at weight 400 that wraps without truncation using overflow-wrap anywhere and min-width 0, keeping headings and confirmation actions outside; other app compositions include grouped messages or inline navigation). Typography is Open Sans; the body is 12px font size, 16px line height, weight 400; emphasis is a plain strong element. In the light theme the container fill is #fff0e8 (--flowx-orange-50) with a 1px #fed1b9 (--flowx-orange-100) border, the icon is #fd6b1c (--flowx-orange-500), the body text is #5b6a7e (--flowx-neutrals-600), and there is no title and no action color. The dark theme is identical to light, because the rendered component uses the same palette tokens in both themes and globals.css does not redefine --flowx palette variables under .dark. The site previews it on a #f7f8f9 (--flowx-neutrals-50) surface.

Placement: put it above the content affected by the consequence, before the person continues, and keep it visible while that consequence applies, stating the consequence before asking people to continue; follow the containing column by filling the available panel or column, since placement width is not a size variant; keep section headings such as Breaking Changes and confirmation actions such as Import Anyway outside the message in the surrounding modal or section; keep field-specific instructions and validation with their control and use a callout only for context affecting a section or task. The use case is Explain the consequence (say what will happen and what will be affected before people continue). Do emphasize the key risk by bolding only the essential consequence and keeping the rest easy to scan; do let longer messages wrap with the baseline icon at the top and the full message shown without truncation; do follow the containing column. The shared inline-message base adds two do items (keep context beside the affected content, with an Info tip beside the settings it explains and a warning above the affected content; keep essential information visible, since a brief toast cannot replace persistent guidance) and two don't items (do not infer one universal style, because the shared Info tip and the page-authored warning differ in spacing and typography and similar call sites contain local variations; do not promote field help into a banner), though the Warning message page renders only its own four items. Considerations: the reference is page-authored markup in the import modal with no shared variant or size API; the Breaking Changes heading and Import Anyway action sit outside the message; state the consequence before asking people to continue and keep the warning visible while it applies; the baseline has no timer or close control. Accessibility: static context needs no live role and dynamic feedback is announced according to urgency; Tab keeps interactive links in the normal reading order and focus does not move merely because a result appears; decorative icons are aria-hidden, aria-describedby can associate the message with an affected control, and no live region is declared so callers must announce important dynamic changes themselves. There are no decision log entries and no known exceptions. Related components are info-message, toast, input-field, and button. Gaps: no dark-mode treatment is defined; no title element exists; no link or action color is defined because the reference banner has no inline action and the spec defines no Action element; the strong emphasis has no numeric weight in the sources and is a documentation recommendation; no hover, focus, or pressed behavior is defined because the message has no interactive parts; there is no Figma source; the spec and rendered component agree on every value.
