Info message (slug info-message, family inline-message, sibling warning-message, status beta, last updated 2026-09-08) is persistent guidance beside the settings or content it explains. It is a source-based recreation of the Designer app's shared idea helper box (shared/components/idea-helper-box/idea-helper-box.component.ts), reviewed against Designer checkout 855605bdb7 on 2026-09-08, not a Figma extraction, and it has no Figma link. Its props are content (projected content, plain text or caller-authored markup; the base wrapper uses 12px / 18px regular text and nested elements can override it), icon (optional projected icon, defaulting to an outlined yellow lightbulb; no custom icon was found in the reviewed call sites and icon size otherwise inherits from context), placement (caller layout: full, half, or content width and surrounding margins, which are layout choices rather than size variants), and width ("fill" or "fit", default "fill", rendered as width 100% or fit-content with max-width 100% and min-width 0). The documentation preview offers content (Plain, Action, Emphasis; default Plain), message width (Fill, Fit; default Fill), and preview width (Wide, Narrow; default Wide, where Narrow caps only the documentation canvas at 320px). Content variations are Plain (default copy "Provider and Webhook Behavior are a one time selection. Once saved these cannot be changed."), Action (a short inline button or link in Blue 500 inheriting the body font, such as "Email Triggers can be activated from Manage Triggers in Runtime"), Emphasis (selective bold on the essential words, such as "Once saved it cannot be modified"), and Short ("Files are combined in the order shown (min. two files)", used for the width examples). Width variations are Fill (100% of the containing column) and Fit (fit-content capped at the container width). There is no title slot and no dismiss control or timer. Nine observed app variations exist in Designer (shared Info tip still at 4px radius, inline Blue 500 link, muted Neutral 500 body in Invite User, 14px / 20px content with a Manage Triggers button in email settings, half-width and content-width hosts that hide when empty, separate 12px / 16px Neutral 500 settings tips with centered icons and 4px radius, a React file-combination tip with a centered 16px lightbulb in an 18px line box, other lightbulb treatments with 6px corners or Neutral 600 text, and a dark document-processing notice with Neutral 700 fill and Neutral 200 text); these are caller differences, not a supported variant API. The message has no interactive states (an inline action underlines on hover and shows a 2px focus-visible outline with 2px offset) and no size axis.

Its anatomy is a Container (gray surface, 8px padding on every side, 8px corner radius, no border, horizontal flex row aligned to the top with an 8px gap, borderWidth 0), an IconSlot (fixed 16px wide and 22px high, flex-centered, non-shrinking), an Icon (Phosphor Lightbulb at regular weight, 16x16, aria-hidden), and a Body (12px / 18px text at weight 400 that wraps without truncation using overflow-wrap anywhere and min-width 0, supporting inline links or buttons and selective bold emphasis while the icon stays at the top of its fixed slot). Typography is Open Sans; the body is 12px font size, 18px line height, weight 400; the action inherits the body font and underlines on hover; emphasis is a plain strong element. In the light theme the container fill is #f7f8f9 (--flowx-neutrals-50) with no border, the icon is #feb913 (--flowx-yellow-500), the body text is #475263 (--flowx-neutrals-700), the action text is #006bd8 (--flowx-blue-500), and there is no title. The dark theme is identical to light, because the rendered component uses the same palette tokens in both themes and globals.css does not redefine --flowx palette variables under .dark. The site previews it on a white surface (#ffffff, --flowx-grays-50) so the gray message background stays visible.

Placement: put it beside the settings or content it explains and keep it visible while its condition applies; fill the column or fit the content without exceeding the available width (callers pick full, half, or content width and margins, and these are not size variants); keep field-specific instructions and validation with their control and use a callout only for context affecting a section or task; observed callers use half-width hosts, content width with max-width 100%, and hide the helper when there is no content. The use case is Explain a setting (clarify how a setting works or what happens when it changes). Do link to the relevant destination with a short, keyboard-accessible inline action; do emphasize only the essential words, keeping 12px / 18px text and allowing wrapping; do choose width to suit the context. The shared inline-message base adds two do items (keep context beside the affected content, with an Info tip beside the settings it explains and a warning above the affected content; keep essential information visible, since a brief toast cannot replace persistent guidance) and two don't items (do not infer one universal style, because the shared Info tip and the page-authored warning differ in spacing and typography and similar call sites contain local variations; do not promote field help into a banner), though the Info message page renders only its own four items. Considerations: the underlying idea helper also implements a borderless orange Warning type that is separate from this reference; check inherited text and icon sizes when projecting links or nested markup, since email settings overrides both with larger text; use a contrasting preview surface; the baseline has no timer or close control. Accessibility: static context needs no live role and dynamic feedback is announced according to urgency; Tab keeps interactive links in the normal reading order and focus does not move merely because a result appears; decorative icons are aria-hidden, aria-describedby can associate the message with an affected control, and no live region is declared so callers must announce important dynamic changes themselves. Decision log: on 2026-09-08 the Info message adopted an 8px radius by confirmed design direction, replacing the 4px radius still observed in the app. There are no known exceptions. Related components are warning-message, toast, input-field, and button. Gaps: no dark-mode treatment is defined; no title element exists; the strong emphasis has no numeric weight in the sources; the action's corner radius is only the Tailwind class rounded-sm and its focus outline color is the theme default; no hover text color is defined for the action; no color is defined for a custom projected icon; there is no Figma source; the spec and rendered component agree on every value, the only documented deviation being the 8px versus 4px app radius.
